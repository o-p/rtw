import Vue from 'vue';
import Vuex from 'vuex';

import Connection from './Connection';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Connection,
  },
});
